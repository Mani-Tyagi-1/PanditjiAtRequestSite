// Loaded on demand by <LazyMotion>: keeps the animation feature bundle (drag,
// layout, gestures) out of the initial JS. Components use the slim `m` element.
export { domMax as default } from "framer-motion";
